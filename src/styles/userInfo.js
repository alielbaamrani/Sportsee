import styled from "styled-components";

const Container = styled.div``;

const Text = styled.h1`
  font-weight: 600;
  font-size: 48px;
  margin-bottom: 40px;
`;

const UserName = styled.span`
  color: red;
`;

export { Container, Text, UserName };
